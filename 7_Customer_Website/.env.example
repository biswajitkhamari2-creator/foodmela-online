# ── Copy to .env.local (gitignored). Never commit real values. ──
# Backend base URL (Express + Upstash Redis). Falls back to production if unset.
VITE_BACKEND_URL=https://food-mela-backend.vercel.app
